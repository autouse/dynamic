//  ! ios 12 beta 2 only debug

// top
extension single {
    
    // style
    
    func infinity () {
            let some = now()
        init(error:Any?) {
            let we = function(at: nil ) () // \\ -- || ** __ ** xx .. §
        }
    } // rule
    
}

// end

// .
